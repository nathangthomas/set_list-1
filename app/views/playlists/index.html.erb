<h1>Playlists!</h1>

<% @playlists.each do |playlist| %>
  <section id="playlist-<%= playlist.id %>">
    <h3><%= playlist.name %></h3>
    <p>Songs:</p>
    <ul>
      <% playlist.songs.each do |song| %>
        <li><%= song.title %></li>
      <% end %>
    </ul>
  </section>
<% end %>
