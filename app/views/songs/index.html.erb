<h1>Songs</h1>
<p><%= flash[:success] %></p>
<% @songs.each do |song| %>
  <section id="song-<%= song.id %>" class="song">
    <h2><%= song.title %></h2>
    <p>Playcount: <%= song.play_count %></p>
    <%= button_to "Add Song", cart_path(song), method: :patch %>
  </section>
<% end %>
